# tagged_tape

Types fade. Values remain, and every value leaves a tag.

## Run

The provided executable targets Linux x86-64.

```sh
chmod +x tagged_tape
./tagged_tape
```

Enter one flag in the form `KCTF{...}`. The program prints `Correct.` only for the exact flag.
