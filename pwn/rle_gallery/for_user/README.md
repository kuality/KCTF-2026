# RLE Gallery

ASCII 작품의 제목과 RLE 본문을 전시하는 갤러리입니다.

```sh
docker compose up --build
nc 127.0.0.1 20003
```

분석용 실행 파일과 정확히 일치하는 Ubuntu 26.04의 `libc.so.6`,
`ld-linux-x86-64.so.2`가 함께 제공됩니다. 로컬 컨테이너에는 명백한 가짜
플래그만 들어 있습니다.
